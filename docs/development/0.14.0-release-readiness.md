# SwiftFTR 0.14.0 Release Readiness

This document is the release gate for SwiftFTR 0.14.0. The release must be tagged from the
release-preparation PR's merge commit on `main`, not from the branch commit.

## Status

SwiftFTR 0.14.0 was published on 2026-07-22 from annotated tag `v0.14.0` at
`46de63af715bc4dab334a343103dba228640a0ab`. The
[release workflow](https://github.com/Network-Weather/SwiftFTR/actions/runs/29957413387), assets,
checksum, SBOM, provenance attestations, curated notes, and remote exact-tag consumer all passed.
The later docs-only merge `0abaaf89d30305beb78940f413345050a3e1b733` updates the public guidance
without changing the tagged release.

## Shipped in the release-preparation change

- [x] Add a coherent `0.14.0 — 2026-07-22` section to `CHANGELOG.md` covering merged PRs #25–#53.
- [x] Publish a DocC migration guide and link it from the documentation landing page and README.
- [x] Set `swiftFTRVersion` and the README SwiftPM requirement to 0.14.0.
- [x] Raise the SwiftIP2ASN floor to 0.4.1 for its July 2026 database and cache-robustness fixes.
- [x] Preserve the v0.13 callable `TraceResult`, `UDPProbeConfig`, and `udpProbe` shapes with
  compatibility overloads, compile-time regression tests, and an external-package consumer.
- [x] Reduce the API compatibility diagnostic to the two intentional additive `DNSError` cases.
- [x] Update GitHub-hosted CI, DocC, and release workflows to current Node 24 action generations.
- [x] Disable the SBOM action's implicit artifact/release uploads so the release contains one
  explicitly named CycloneDX SBOM.
- [x] Require an existing annotated semantic-version tag and matching CLI version before release
  assets can be published; documentation continues to deploy only from the reviewed `main` branch.
- [x] Correct the maintainer release instructions in `CONTRIBUTING.md`.

## Required before tagging

- [x] Replace `Unreleased` in the 0.14.0 changelog heading with the release date.
- [x] Run `swift format lint --strict -r Sources Tests` with zero diagnostics.
- [x] Run `SKIP_NETWORK_TESTS=1 PTR_SKIP_STUN=1 swift test -c debug`.
- [x] Run debug and release builds, including the `swift-ftr` product.
- [x] Build `IntegrationPackage` and `TestLibraryUsage` as external consumers.
- [x] Generate DocC with warnings treated as errors.
- [x] Validate every changed workflow with `actionlint` and run `git diff --check`.
- [x] Run `swift package diagnose-api-breaking-changes v0.13.0 --products SwiftFTR`; accept only
  `DNSError.invalidTimeout` and `DNSError.setsockoptFailed`. The command is expected to exit 1
  because Swift's API digester treats additive enum cases as source-breaking.
- [x] Verify `.build/release/swift-ftr --version` and JSON output both report 0.14.0.
- [x] Verify `swift-ftr probe tcp` exits zero for reachable open and closed ports and nonzero for
  timeout and stale-interface results while preserving its structured output.
- [x] Run the live acceptance matrix below. The network paths were exercised on commit `4105997`;
  the only subsequent executable change propagates an already-produced TCP failure result to the
  CLI exit status and was verified separately against reachable, closed, timeout, and stale routes.
- [x] Merge the release-preparation PR and wait for main CI and DocC deployment to pass.
- [x] Repeat the version, API-diff, and live smoke checks on the resulting mainline merge commit.

## Live acceptance matrix

Discover interface names and addresses from the operating system for every run. Never infer a
physical or VPN role from a BSD-name prefix or numeric suffix.

- [x] Direct dual-stack network (validated again on `46de63a` on 2026-07-22 using OS-discovered
  routes and addresses):
  - default-route IPv4 and IPv6 ping;
  - bound IPv4 and IPv6 ping using the exact selected interface and source address;
  - IPv4 and IPv6 traceroute plus streaming trace;
  - IPv4 and IPv6 TCP probes;
  - bound IPv4 and IPv6 UDP loopback probes;
  - A and AAAA record requests over both IPv4 and IPv6 DNS transports;
  - dual-stack STUN public-address discovery; and
  - embedded IPv4 and IPv6 ASN lookup.
- [x] VPN connected (validated 2026-07-22):
  - record OS-reported default routes and active interfaces;
  - repeat default-route IPv4/IPv6 checks with outcomes interpreted against the VPN policy;
  - verify caller-selected exact-interface probes either use the selected route or fail explicitly;
  - never accept silent fallback to a different interface.
- [x] VPN disconnected again (validated 2026-07-22):
  - confirm default routes and public addresses return to the direct-network baseline;
  - confirm the former provider interface disappears or exact binding to it fails; and
  - repeat the direct IPv4/IPv6 smoke set.

The exact-interface HTTPS work in PR #53 completed an off/on/off ProtonVPN rehearsal with
packet-capture evidence. The concise suite was repeated off/on/off on `46de63a`: IPv4 used the
OS-selected tunnel while connected, Proton's IPv6 suppression produced explicit no-route results,
the stale tunnel selection failed after disconnect, and direct IPv4/IPv6 service returned. All 16
HTTPS spike tests passed in both reachable states; the connected IPv6 live case skipped because the
OS reported no reachable IPv6 primary.

The checked deterministic gates above were rerun on the release-preparation working tree after the
final code and documentation edits. The offline suite completed with 287 Swift Testing cases plus
the XCTest suites and no failures. Debug and release builds were warning-free. The version,
API-diff, and concise live checks were then repeated on merge commit `46de63a` before tagging.

## Tag and publication sequence

1. [x] Create an annotated `v0.14.0` tag on the release-preparation merge commit.
2. [x] Push only that tag and watch `Release (attested + SBOM)` complete.
3. [x] Verify the ARM64 CLI, SHA-256 file, single `sbom.cdx.json`, and provenance attestation.
4. [x] Replace auto-generated GitHub notes with the curated 0.14.0 changelog section and add the
   `v0.13.0...v0.14.0` comparison link.
5. [x] Confirm the checksum matches the published binary.
6. [x] Build a throwaway external Swift package using the remote repository with `exact: "0.14.0"`.
7. [ ] Update NWX to the published tag, refresh package resolution, and run its SwiftFTR integration
   tests before broad rollout.

## Explicitly not blocking 0.14.0

- The exact-interface `NWConnection` HTTPS implementation remains an internal experiment. It is not
  wired into public `httpProbe`, so the production HTTP Probe roadmap is follow-up work rather than
  a 0.14.0 release gate.
- DNS-over-TCP fallback for truncated UDP replies remains unimplemented and is documented in the
  changelog and migration guide.
- Multipath remains intentionally IPv4-only.
- The self-hosted `local-integration-tests` workflow still uses its older checkout action until the
  dormant runner is upgraded and validated for the Node 24 Actions runtime. Local execution of the
  live matrix is the 0.14.0 gate.
- Historical release notes retain literal interface names where they record past observations.
  Current examples, discovery, fixtures, and runtime logic do not assign roles from those names.

## Follow-up after 0.14.0

- Execute the six-PR [HTTP Probe production plan](HTTPProbe-Production-Roadmap.md) before exposing
  exact-route HTTP as public API.
- Add DNS-over-TCP fallback with the same response-correlation and resource-bound guarantees as the
  UDP implementation.
- Restore and validate the self-hosted integration workflow before depending on it as a release
  gate.
